# Build environment for the NanoTTS C++ runtime.
FROM ubuntu:24.04

RUN apt-get update && apt-get install -y --no-install-recommends \
        build-essential cmake ninja-build git ca-certificates curl pkg-config \
        libssl-dev \
        libnuma-dev libmp3lame-dev \
    && rm -rf /var/lib/apt/lists/*

# ONNX Runtime, prebuilt. 1.30.0 is the current release; the CPU tarball is small.
ARG ORT_VERSION=1.30.0
RUN curl -sL -o /tmp/ort.tgz \
      "https://github.com/microsoft/onnxruntime/releases/download/v${ORT_VERSION}/onnxruntime-linux-x64-${ORT_VERSION}.tgz" \
    && mkdir -p /opt/onnxruntime \
    && tar -xzf /tmp/ort.tgz -C /opt/onnxruntime --strip-components=1 \
    && rm /tmp/ort.tgz
ENV ONNXRUNTIME_ROOT=/opt/onnxruntime

WORKDIR /src
